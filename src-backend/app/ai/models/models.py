# Expose models from app.models.ai_models for single source of truth database compatibility
from app.models.ai_models import AIConversation, AIMessage, AILog, AIMemory, AISession, AISetting, AIPromptTemplate
